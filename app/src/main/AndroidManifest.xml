<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.com.bitscube_intellectual">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TranslucentTheme">
        <activity
            android:name=".ui.activity.SplashActivity"
            android:exported="true"
            android:launchMode="standard"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.GuideActivity"
            android:launchMode="singleTask"
            android:theme="@style/TranslucentTheme" />
        <activity
            android:name=".ui.activity.LoginActivity"
            android:theme="@style/LoginTheme" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:launchMode="singleTask"
            android:theme="@style/TranslucentTheme" />
        <activity
            android:name=".ui.activity.SearchActivity"
            android:theme="@style/TranslucentTheme" />
        <activity
            android:name=".ui.activity.ThinkTankTalentsInfoActivity"
            android:theme="@style/TranslucentTheme" />
        <activity
            android:name=".ui.activity.ThinkTankDeptInfoActivity"
            android:theme="@style/TranslucentTheme" />
        <activity
            android:name=".ui.activity.ThinkTankProjectsInfoActivity"
            android:theme="@style/TranslucentTheme" />

        <activity android:name=".ui.activity.MemberListActivity"
            android:theme="@style/TranslucentTheme"/>

        <activity
            android:name=".ui.activity.MyProjectActivity"
            android:theme="@style/TranslucentTheme" />
        <activity
            android:name=".ui.activity.MyEditTagsActivity"
            android:theme="@style/TranslucentTheme" />
        <activity
            android:name=".ui.activity.MySettingActivity"
            android:launchMode="singleTask"
            android:theme="@style/TranslucentTheme" />
        <activity
            android:name=".ui.activity.MyAppVersionActivity"
            android:theme="@style/TranslucentTheme" />
    </application>

</manifest>